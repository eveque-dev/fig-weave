# Python
# **不带斜杠**：带斜杠只匹配目录，而这个仓库里 `.venv` 一度是个**被提交进去的
# 符号链接**（mode 120000，内容指向它自己的绝对路径），`.venv/` 完全挡不住它。
# 后果不是「多了个文件」——git 会在 stash pop / checkout / 目录改名时忠实地把
# 那个自引用链接恢复回来，**当场顶掉开发者真正的 venv**，然后
# `.venv/bin/python` 报 "too many levels of symbolic links"。两天内踩了两次。
.venv
__pycache__/
*.pyc

# 打包产物
/dist/
/build/
*.egg-info/

# Windows 桌面版的内置渲染 runtime（scripts/build_worker_runtime.py 生成）：
# 200 MiB 上下的二进制，随安装包分发但**绝不进 Git**。
# 版本锁在 packaging/runtime-lock.json 里，那个才是要提交的东西。
/runtime/

# 前端构建产物（scripts/build_frontend.py 生成，随 wheel 分发但不进仓库）
/src/tavotto/web/
web/node_modules/
web/dist/
web/test-results/
web/playwright-report/
# 从仓库根跑 `npx playwright test` 时产物落在根上，同样挡住
# （曾被 `git add -A` 顺手收进一次提交）
test-results/
playwright-report/

# 运行时数据：装成包后一律落在用户数据目录
# （macOS ~/Library/Application Support/Tavotto、Linux ~/.local/share/tavotto、
#  Windows %LOCALAPPDATA%\Tavotto）。下面几个是旧版本在仓库里留下的残余。
/cache/
/exports/
/layouts/
/baked_overrides.json
*.log
.pytest_cache/

# Tauri 桌面壳（src-tauri/ 源码进仓库；构建产物与 CLI 生成的 schema 不进）
src-tauri/target/
src-tauri/gen/

# Rust supervisor tavotto-workerd（workerd/ 源码进仓库；构建产物不进）
workerd/target/

# CI 工具 crate（tools/*，源码进仓库；构建产物不进）
tools/*/target/

# 工具与系统
.gstack/
.DS_Store

# 开发期界面验证截图：含真实研究图，不进公开仓
/docs/verify/

# 内部执行清单 / 路线图，不进公开仓。
# 旧名一并保留：这两条挡的是工作副本里的真实文件，改名不等于用户本机的文件也改了名。
/docs/MAGPLOT_RENAME_AND_MATURITY_PLAN.md
/docs/TAVOTTO_RENAME_AND_MATURITY_PLAN.md
.impeccable/

# CodeGraph 本地索引（可再生）
.codegraph/

# 品牌源文件（设计稿，随手放在工作副本里；产物由 scripts/build_brand_assets.py 生成）
/logo.ai
/logo.pdf
/Magplot Brand System.html
/Tavotto Brand System.html

# Codex 内嵌画布（scripts/build_mcp_widget.py 生成）：**不进 git**（ADR 0043）。用户装到的
# 来自发行分支 plugin-stable，由 CI 从固定源码状态构建、验证、发布；本地构建一次即可试用。
# web/dist-mcp/ 是旧版构建器的中间目录（现在写临时目录），留着挡住旧工作副本里的残留。
codex-plugin/mcp/widget/canvas.html
web/dist-mcp/
web/dist-playground/
web/dist-site/
web/dist-r/

# 冒烟 / 手工跑示例项目时，导出与画布布局落在项目自己的 tavottofile/ 里
# （engine/config.project_export_dir）。`git add -A` 会顺手把它收进提交。
/examples/*/tavottofile/

# 遥测代理的部署元数据（Vercel CLI 生成，含本机链接的项目 id）
/services/telemetry_proxy/.vercel/

# 验收 corpus 的产物。visual_regression.py 会把 corpus 复制到临时目录再跑，
# 正常流程不会在这里落文件；但有人手工跑一遍脚本调试时会，而一堆 PDF 混进
# 版本库既没意义又会让「工作目录干净」的判定失效。
# **基线（tests/acceptance/baselines/*.png）不在此列**——它是需要 code review
# 的资产，必须随代码走。
/tests/acceptance/corpus/*.pdf
/tests/acceptance/corpus/*.png
