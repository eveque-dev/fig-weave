# 这里列的提交是**纯机械的全仓重排**，不含任何人写的改动。
# git blame / GitHub 的 blame 视图会跳过它们，指向真正写下那行代码的提交。
#
# 本地要生效需要各自配一次（GitHub 网页版自动读根目录下的这个文件，不用配、
# 也没有任何仓库设置要开）：
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# **必须写全 40 位小写十六进制。** 两种写错的后果不一样，实测过：
#   * 写缩写（`091751c`）→ `git blame` 直接 `fatal: invalid object name`，
#     配了这个文件的人**所有 blame 都用不了**；
#   * 写一个不存在的全长 SHA → git **静默什么都不跳过**（exit 0），
#     文件看着有这一行，实际一点用没有——这种更难发现。
# `tests/test_blame_ignore_revs.py` 把这两条都变成红灯。
#
# 只往这里加**确实只有工具产出、没有一行人工改动**的提交——夹带了别的东西，
# blame 会连那些一起忽略掉，而那正是你将来最想查到的部分。

# Ruff formatter 基线迁移（205 个文件，AST 对拍语义差异 0）
7d0f98b7ea4a963174341a8110467d69cf0a67f7
