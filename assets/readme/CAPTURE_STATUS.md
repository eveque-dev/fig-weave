# Outward-facing product images — inventory and capture status

Written 2026-09-19 for the website / README update v2. Every image the product
repository, the website and the plugin documentation show of the product, where
it came from, and whether it shows the product as it ships now. Update it when
a capture is re-run; a row that says `stale` is a debt, a row that says
`blocked_missing_capture` is a debt with a reason.

Capture environment for the `current` rows: `main` @ `85820bff` (v0.15.0 plus
six backend PRs; the interface is v0.15.0's), frontend built from that tree
(`python scripts/build_frontend.py`), `assets/readme/capture.mjs` against a
throwaway profile with the bundled Playwright Chromium, 1440 × 800 at 1.5×,
`examples/figures/` as the project, interface language forced per set
(`en-US` / `zh-CN`). Nothing staged, nothing retouched: the shots are what the
script clicked into being.

| Image | Used by | Shows | Captured | Status |
| --- | --- | --- | --- | --- |
| `workbench.png` / `workbench.zh.png` | both READMEs (first screenshot); website `src/assets/product/` (synced copy, the social card is cropped from it) | inside figure (a): element tree, page with three panels, the title's properties | 2026-09-19 · main @ 85820bff | `current` — website copy needs `pnpm sync-assets` + `pnpm make-og` after this lands |
| `layout.png` / `layout.zh.png` | both READMEs (Build the page) | layout mode: asset library, the composed 150 × 112.5 mm page with (a) selected, its position / size / scale | 2026-09-19 · main @ 85820bff | `current` |
| `preflight.png` / `preflight.zh.png` | both READMEs (Export for publication) | the export dialog with the run's own checks: 6 blocking · 4 warning · 2 not verifiable · 3 suggestion, the first group's findings with values, the written-confirmation box | 2026-09-19 · main @ 85820bff | `current` — the shot is the whole dialog now, not a cropped list; no "all clear" was staged |
| `hero.svg` | both READMEs (banner) | brand lockup, the brand line, a drawn miniature of the editor | hand-authored; brand line updated 2026-09-19 | `current` (illustration, not a screenshot — it does not claim to be one) |
| `workflow.svg` / `workflow.zh.svg` | both READMEs (Finish figures an AI wrote) | the script → matplotlib → Tavotto → PDF/PNG diagram | hand-authored 2026-08-20 | `current` (diagram) |
| `launch-film-cover.webp` / `launch-film-cover-en.webp` | `README.zh-CN.md` / `README.md` (link to the film on tavotto.com, `/zh/#video` and `/#video`) | the film's last frame in each language: the mark on a dark ground with its tagline | frame at 31.5 s of the owner's finished cuts, 2026-09-19 | `current` — covers, not product screenshots; never to be used as one |
| `docs/acceptance/codex-desktop-tavotto-2026-08-24.png` | `docs/acceptance/codex-desktop-canvas.md` | the in-task canvas inside a real Codex Desktop, with the host's own frame around it | 2026-08-24 · engine/plugin 0.9.2 · Codex Desktop 26.818.41509 | `stale` as evidence for v0.15.0 — the acceptance it documents has not been re-run for the current release. Re-running needs a real Codex Desktop session following that document; nothing in CI can produce it. Until then the READMEs say exactly that and show no newer canvas image |
| Codex canvas, current release (`codex-canvas`) | wanted by the website update v2 (§4.3) | the current host frame, the embedded canvas, a real selection | — | `blocked_missing_capture`: requires a real Codex Desktop task on a machine with the plugin from this release installed; the desktop-window handoff, a browser tab and the MCP protocol tests are not this image and must not be labelled as it |
| Desktop shell (Tauri window chrome) | website update v2 §4.3 "workbench" wants the desktop main window | the same editor inside the packaged app's window | — | `blocked_missing_capture`: the README shots are the editor in a browser tab (what `capture.mjs` can drive); the packaged app's window chrome is one macOS title bar away and was not captured here. If a shot of the packaged app is wanted, it is a manual capture from an installed release with the version recorded |
| `assistant` / `environment` / `onboarding` | website update v2 §4.3 (documentation images) | — | — | not referenced by any current document; nothing to replace. Listed so the absence is a decision, not an oversight |
| website `public/og/og.png` / `og-zh.png` | social cards | brand lockup + brand line + a crop of `workbench(.zh).png` | generated by `scripts/make-og.mjs` | regenerate after the synced copy updates (see first row) |
| website `src/assets/story/*` | the homepage's story | the real figure, real overrides, real composed page, real preflight runs, real exports | generated on the shipped runtime (ADR-0002 there) | out of scope here: not screenshots, and governed by the website's own `check-story-assets` |

Known, unchanged since the 2026-08-20 set: in the inside-the-figure view the
panel frame clips the bottom of figure (a) — the x-axis label is cut — because
the figure is drawn at its figure size (80 × 57.6 mm) inside the frame the
panel was placed at. It was in the previous `workbench.png` too. It is the
product's behaviour, photographed as is; if it is a defect it is one for the
product, not for the screenshot.
