# Flask 父进程依赖（.venv）。注意：这里刻意没有 matplotlib——
# engine/registry.py、pool.py、ai_bridge.py 必须保持纯标准库可 import，
# 科学栈由 worker 解释器提供（pool.find_worker_python / TAVOTTO_WORKER_PYTHON）。
flask==3.1.3
pymupdf==1.28.2
