{
  "schema": 1,
  "note": "支持矩阵的唯一权威文件（1.0 审计 P1-06）。README、发布页、官网、应用内文案都以这里为准；tests/test_support_matrix.py 逐条与 pyproject / runtime-lock 对拍，防止承诺与事实分叉。status 语义：supported = 有安装包 + CI 门禁 + 真产物冒烟；beta = 可用但验收面没到 supported；unsupported = 不构建不承诺（能跑也别写进宣传）。label_en / en 是发布页用的英文成文（scripts/make_release_support_section.py 渲染进每个 Release body）——状态词由 status 派生，en 只写渠道与出路，不写支持等级。",
  "python": {
    "requires": ">=3.10,<3.15",
    "tested": ["3.10", "3.11", "3.12", "3.13", "3.14"],
    "note": "与 CI 矩阵一致（backend-fast 跑 3.10 / 3.13 / 3.14，package 的 wheel 冒烟多一档 Linux 3.14；tests/test_support_matrix.py 钉住 tested 的最高档必须在 backend-fast 里有腿）。3.14 于 2026-09-14 放开（issue #33）：Homebrew / pipx 默认解释器已是 3.14，而只有没写上界的 0.8.0 能被 3.14 的 pip 解析到。桌面版内置 runtime 仍是 3.13（packaging/runtime-lock.json），放开上界不等于换发行 runtime。"
  },
  "targets": [
    {
      "id": "windows-x64-desktop",
      "label": "Windows x64 桌面版",
      "label_en": "Windows x64 desktop",
      "status": "supported",
      "channel": "NSIS 安装包（含内置渲染 runtime 与 Rust supervisor）",
      "en": "NSIS installer with a bundled rendering runtime — download the `.exe` below. Whether this release's installer is code signed is stated in the section underneath; an unsigned installer makes Windows show a SmartScreen prompt on first run.",
      "gates": "windows-exe-smoke + desktop-tauri 真产物冒烟 + nightly 安装/升级/卸载链路"
    },
    {
      "id": "macos-arm64-desktop",
      "label": "macOS Apple Silicon 桌面版",
      "label_en": "macOS Apple Silicon desktop",
      "status": "supported",
      "channel": "dmg（签名 + 公证；含内置渲染 runtime）",
      "en": "Signed and notarized `.dmg` with a bundled rendering runtime — download the `.dmg` below.",
      "gates": "macos-app-smoke + 签完名的 .app 从中文+空格路径冒烟"
    },
    {
      "id": "macos-x86_64",
      "label": "macOS Intel",
      "label_en": "macOS Intel",
      "status": "unsupported",
      "channel": "pip / pipx（浏览器模式）",
      "en": "No desktop build (never built or smoke-tested on Intel hardware). Use the PyPI browser mode instead: `pipx install \"tavotto[worker]\"`.",
      "note": "桌面产物没构建过也没冒烟过（runtime-lock 里 shipped=false，CI 无 Intel runner）。宣传物料不得写「支持 Intel」。"
    },
    {
      "id": "windows-arm64",
      "label": "Windows ARM",
      "label_en": "Windows ARM",
      "status": "unsupported",
      "en": "No native build and no real-hardware or emulation verification.",
      "note": "无原生产物与真机/仿真验收。"
    },
    {
      "id": "linux",
      "label": "Linux",
      "label_en": "Linux",
      "status": "beta",
      "channel": "pip / pipx（浏览器模式；会话认证默认启用，ADR 0008）",
      "en": "No desktop installer. Browser mode from PyPI: `pipx install \"tavotto[worker]\"`, then `tavotto` opens the editor in your browser.",
      "note": "无桌面安装包，不写 desktop support；CI 的 backend/package 矩阵在 Linux 上最全，但没有发行安装物验收。"
    }
  ]
}
